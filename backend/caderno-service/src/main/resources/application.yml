server:
  port: 8082

spring:
  application:
    name: caderno-service
  datasource:
    url: jdbc:h2:mem:cadernodb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

cors:
  allowed-origins: http://localhost:4200,https://*.vercel.app
